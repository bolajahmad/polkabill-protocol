services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: squid
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d squid"]
      interval: 5s
      timeout: 5s
      retries: 5
    # Uncomment for logging all SQL statements
    # command: ["postgres", "-c", "log_statement=all"]
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: polkabill-squid
    environment:
      - DB_NAME=squid
      - DB_PORT=5432
      - DB_HOST=db
      - DB_PASS=postgres
      - GQL_PORT=4350
      - RPC_BASE_SEPOLIA_HTTP=${RPC_BASE_SEPOLIA_HTTP}
      - RPC_ETHEREUM_SEPOLIA_HTTP=${RPC_ETHEREUM_SEPOLIA_HTTP}
      - RPC_ASSETHUB_HTTP=${RPC_ASSETHUB_HTTP}
      - SQD_ALLOW_INCONSISTENT_DATA=${SQD_ALLOW_INCONSISTENT_DATA}
      - SQD_EVM_MAX_BATCH_CALL_RETRIES=${SQD_EVM_MAX_BATCH_CALL_RETRIES}
      - SQD_EVM_BATCH_CALL_RETRY_DELAY=${SQD_EVM_BATCH_CALL_RETRY_DELAY}
    ports:
      # GraphQL endpoint at port 4350
      - "4350:4350"
    command: ["sqd", "serve:prod"]
    depends_on:
      db:
        condition: service_healthy
  processor:
    image: polkabill-squid
    environment:
      - DB_NAME=squid
      - DB_PORT=5432
      - DB_HOST=db
      - DB_PASS=postgres
      - RPC_BASE_SEPOLIA_HTTP=${RPC_BASE_SEPOLIA_HTTP}
      - RPC_ETHEREUM_SEPOLIA_HTTP=${RPC_ETHEREUM_SEPOLIA_HTTP}
      - RPC_ASSETHUB_HTTP=${RPC_ASSETHUB_HTTP}
    ports:
      # prometheus metrics exposed at port 3500
      - "3500:3500"
    command: ["sqd", "process:prod"]
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data: